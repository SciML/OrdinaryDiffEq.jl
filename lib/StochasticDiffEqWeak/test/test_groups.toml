[Core]
versions = ["1.11", "1", "pre"]

[WeakConvergence1]
versions = ["1"]

# WeakConvergence2, 3, 6 run on buildkite (heavy Monte Carlo, 240min timeout)
# WeakConvergence4 runs on buildkite (needs 360min timeout)
# WeakConvergence5 runs on buildkite (needs exclusive runner)

[IRI1WeakConvergence]
versions = ["1"]

[WeakAdaptiveCPU]
versions = ["1"]

[QA]
versions = ["1"]
