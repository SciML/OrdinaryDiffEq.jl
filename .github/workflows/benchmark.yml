name: Benchmark this PR
on:
  pull_request:
    branches:
      - master
    paths-ignore:
      - 'docs/**'

permissions:
  pull-requests: write    # needed to post comments

# Prevent cancellation when new commits are pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: false

jobs:
  bench:
    name: "Benchmarks"
    timeout-minutes: 30
    strategy:
      matrix:
        version:
          - "1"
          - "lts"
    runs-on: ubuntu-latest
    env:
      # Force consistent Julia depot path for self-hosted runners
      JULIA_DEPOT_PATH: ~/.julia
    steps:
      # Clean stale AirspeedVelocity installation to avoid path issues with self-hosted runners
      - name: Clean AirspeedVelocity cache
        run: |
          rm -rf ~/.julia/bin/benchpkg* ~/.julia/bin/asvjl* || true
          rm -rf ~/.julia/packages/AirspeedVelocity || true
          rm -rf ~/.julia/compiled/*/AirspeedVelocity || true
          # Ensure ~/.julia/bin is in PATH
          echo "$HOME/.julia/bin" >> $GITHUB_PATH
      - uses: MilesCranmer/AirspeedVelocity.jl@action-v1
        with:
          julia-version: "${{ matrix.version }}"
          script: "benchmark/benchmarks.jl"
          extra-pkgs: "StableRNGs,StaticArrays"
